window.document.addEventListener("DOMContentLoaded", init);
function init() {

    //STEP 8
    
    function delayLoad() {
        window.resizeTo (500,300);
    };
    window.setTimeout(delayLoad, 2000);

};